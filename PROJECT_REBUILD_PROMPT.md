# Paul Wei Hyperliquid 追踪项目重做 Prompt

你要从零设计并实现一个本地研究项目，用来长期追踪 Paul Wei 在 Hyperliquid 上的 BTC 永续合约交易行为。请发挥你的创造力，不要把下面的要求理解成固定实现方案。重点是满足研究目标、保证数据可靠、让交互体验清晰流畅；具体技术架构、文件命名、前后端形式、图表组织方式都可以由你自行判断。

## 核心目标
- 追踪地址：`0xdae4df7207feb3b350e4284c8efe5f7dac37f637`。
- 能长期保存 Paul 的历史订单、成交、当前挂单、账户价值、仓位和杠杆情况。
- 能和官方 Hyperbot 页面交叉验证当前状态：`https://hyperbot.network/trader/0xdae4df7207feb3b350e4284c8efe5f7dac37f637`。
- 重点帮助用户回答：Paul 在某个历史时间点持有什么仓位、挂了哪些单、账户价值是多少、实际杠杆是多少。
- 项目应该适合长期维护，一周更新一次即可，但要避免重复数据和脏数据。

## 数据需求
- 使用 Hyperliquid 公共 API。
- 需要抓取并保存订单、成交、当前挂单和账户状态。
- 至少需要覆盖这些数据类型：
- `historicalOrders`
- `openOrders`
- `frontendOpenOrders`
- `userFills`
- `userFillsByTime`
- `clearinghouseState`
- `spotClearinghouseState`
- `portfolio`
- BTC K 线数据
- 如果发现还需要其他 Hyperliquid 数据源才能更准确复原历史状态，可以自行加入。
- 原始 API 响应应该尽量保存，方便以后复查。
- 数据库或本地存储需要支持增量更新和去重。

### 历史回补（补回已掉出 API 滚动窗口的早期订单）
- `historicalOrders` 只返回最近约 2000 条、`userFills` 也是滚动窗口，Paul 越早期的订单越可能已经永久取不到。要尽量从多个来源回补，并都按 `oid` / `tid` 去重合并进同一个数据库：
  - **旧项目 / 历史 weekly snapshot**：如果之前用别的脚本存过快照（不同目录结构），写一个一次性导入脚本把它们灌进来。来源路径用环境变量（如 `PAUL_LEGACY_SRC`）或 `--src` 参数指定，不要硬编码。
  - **fills 反推**：fills 里出现过、但 orders 表里没有的 `oid`，至少能从成交记录反推出一个"已成交订单"的最小记录（注意诚实标注：创建时间只能近似为首次成交时间）。
  - **链上完整归档（可选，需成本）**：Hyperliquid 节点的 `node_order_statuses` 含每个用户的完整订单 lifecycle（包括从未成交就撤销、被 rejected 的单）。可从 Artemis 的 requester-pays S3（`s3://artemis-hyperliquid-data`，需 AWS 凭证）或 Zenodo 的公开学术数据集（免费 HTTP 直链，但只覆盖特定月份）导入。这类导入脚本也要把来源、字段映射、成本说清楚。
- 一个直观自检：在做任何重建前，先用最宽时间窗口（`startTime=0`）确认 Paul 的真实首次活动时间（首笔 `userNonFundingLedgerUpdates` 充值 / 首笔 fill），避免误判"几月才建号"。

### 可移植存储（数据可放到大容量盘）
- 跑链上完整归档导入时，临时下载量可能几十～几百 GB，所以数据根目录必须可配置（如环境变量 `PAUL_DATA_ROOT` 指向另一块盘），不能写死在默认 `data/`。
- 提供一个迁移脚本，把现有数据库 + 原始 JSON 搬到新盘并校验一致后，告诉用户怎么设环境变量。
- 所有代码读写数据都要从集中的路径常量出发，**严禁硬编码绝对路径或跟特定用户机器相关的路径**（见仓库 `CLAUDE.md`）。

## 重要字段语义
- `oid` 是订单的关键 id，应该用于订单关联和去重。
- `timestamp` 通常代表订单创建时间。
- `statusTimestamp` 通常代表订单状态变化时间，例如成交或撤单时间。
- `historicalOrders` 可以帮助还原订单生命周期，但不能单独还原完整账户状态。
- `fills` 是重建真实仓位变化的关键。
- `clearinghouseState` 是当前交易所真实仓位和账户价值的关键校验来源。
- `spotClearinghouseState` 是现货余额，不要把它和永续合约账户混在一起。

## 历史重建要求
- 能根据成交记录重建 Paul 的历史 BTC 永续仓位。
- 能根据账户价值估算每个历史时间点的实际杠杆。
- 实际杠杆的核心含义是：仓位名义价值 / 账户总价值。
- 杠杆要带方向：正数代表多头，负数代表空头。
- 例如 `-0.0262` 应理解为约 `2.62% Short`。
- 当前最新状态需要同时展示两套信息：
- 根据 `fills` 重建出来的仓位和杠杆。
- 根据 `clearinghouseState` 得到的交易所真实当前仓位和杠杆。
- 如果两者不一致，要能帮助用户发现并排查原因。

## 订单和挂单要求
- 能查看某个历史时间点 Paul 当时可能仍然 active 的挂单。
- 能区分当前挂单、历史成交单、历史撤单。
- 能展示挂单价格、方向、BTC 数量、名义价值、占账户价值百分比。
- 不要只按当前持仓大小计算订单百分比，用户更关心订单相对整个账户有多大。
- 对于主动成交或类似 market order 的成交，可以设计一种方式让用户提前看到它即将发生，例如在成交前一段时间用不同线型或不同标记展示；具体表现形式由你决定。
- **挂单一旦成交或撤销，对应的图表标记 / 线段必须消失**——已经结束的订单不能继续显示成"还在挂"。一个直观自检：随便选一个早期时间点，所有已知已成交 / 已撤销的订单都应在视觉上消失，不能有残留。
- **挂单线段不要贯穿整张图**。它应该从用户选中的时刻开始向"未来"（图右侧）延伸，到该挂单的结束时间为止；如果该挂单当时仍在挂，则一直延伸到图右端，并在图右端预留一些"未来空白"让线段视觉上有地方画。
- 主图挂单线段的默认值应该是"显示全部活跃挂单"，但要提供切换到 N 大（按账户占比）或完全关闭的控件，避免线太多时遮挡 K 线；右侧表格永远展示完整列表。

### 挂单线视觉编码（多轮打磨后的最终约定）
- **线型表示结局**：实线 = 该挂单最终成交（filled）；点线 = 未成交（包含两种：已被撤销 / 当前仍 open 未来未知）。
- **用点线（Dotted）而不是虚线（Dashed）**：虚线点距大，跨度只有一两根 K 线的短线段经常只渲染半段甚至看不见；点线点距小，再短的线段也能看清。
- **不存在"推断"**：在拿到完整订单 lifecycle 数据后（见"历史回补"），每个订单的状态都是直接观测的事实，不要再加"推断"标签或为推断设计单独样式。早期那种"open 但状态时间戳早于选中时刻 → 推断仍在挂"的逻辑应删除。
- **主图线段上不要叠加任何文字标签**：方向已经用颜色表达（绿=买、红=卖），结局已经用线型表达（实/点），价格已经由价格轴标签表达。数量、占账户百分比、买卖、成交/撤销这些都不要再写在图上——保持图面干净，完整明细放右侧表格。
- 右侧订单表格用彩色 badge 区分每条订单的最终状态（成交 / 在挂 / 撤销）。

### 显示全部订单（总览模式）
- 提供一个开关（默认关），开启后把**所有订单**一次性画在主图上，每条按它**自己真实的生命周期**（创建时刻 → 成交/撤销时刻）画线段，**与当前选中时刻无关**。用来一眼总览 Paul 历史上全部挂单的价格分布与密度。
- 这个模式不受"主图挂单线 N 条上限"约束（那个上限是给单时刻视图防遮挡用的）。
- 关闭时回到默认的"只看选中时刻活跃挂单"视图，且默认视图不能因为这个开关受任何影响。

## K 线和时间粒度
- 用户希望能图标上查看 BTC 的 `1d`、`4h`、`1h` 三种时间粒度。
- K 线数据来源要清楚标注。
- 如果使用 Hyperliquid 的 BTC candle 数据，成交量必须标明是 Hyperliquid 的成交量，不是 Binance 或 Coinbase。

## 图表和交互
- 用户需要一个能流畅查看历史状态的交互图表。
- 鼠标移动到某个时间附近时，应该容易选中对应 K 线。
- 选中某根 K 线后，要能看到该时间点的仓位、杠杆、账户价值和挂单信息。
- 用户希望能固定某个时间点，不要鼠标一移开就跳走。
- 用户希望有类似播放功能，可以按时间向前推进，观察 Paul 挂单和杠杆如何变化。
- 图表上应该能直观看到挂单价格位置，但不要让订单细节遮挡 K 线。
- 订单细节可以放在侧边栏、下方表格或其他清晰区域。
- 图表下方需要展示 Paul 的历史杠杆变化。
- 杠杆图需要清楚区分多头和空头。
- 页面上应清楚标注数据来源和指标含义。
- **fill 标记要区分四种**：被动买、主动买、被动卖、主动卖。即便都是"主动"，买和卖也要用不同颜色，不能合并成一种颜色。
- **提供"只看主动成交"开关**：能一键把所有 maker / 被动 fill 箭头隐藏，只保留 taker / 主动箭头，方便专注分析 Paul 主动发起的成交与挂单之间的关系。
- **fill 标记的文字用"占账户百分比"而不是 BTC 数量**：绝对数量（如 0.012 BTC）没有指导价值，占当时账户价值的百分比（如 5.2%）才能直观看出这笔成交对账户的冲击大小。
- **上下两图（K 线主图 + 杠杆副图）必须严格对齐**，否则鼠标 hover 时上下十字线 / 选中 K 线会错位。两个根本原因都要处理：
  - (1) **数据点序列必须完全一致**：杠杆图和 K 线图的数据点数量、时间值必须逐一对应。缺杠杆值的 bar 不能用 `filter` 丢掉、也不能用 whitespace（会被图表库在序列开头 trim 掉导致点数不一致），要用 0 填充（Paul 进场前 0% 杠杆本就是正确语义）。否则图表库按数据点索引同步可见区间时会整体平移错位。
  - (2) **两图价格轴最小宽度要设成一样**：上图标价格（5 位数）、下图标百分比（3 位数），轴宽不同会让绘图区左右边缘错位 → 时间轴错位。固定一个相同的 `minimumWidth`。
- **一键启动入口**：要提供 IDE 里点绿色播放就能跑起来的入口（例如 `server.py`），自动起 HTTP server + 打开浏览器；不要让用户记一串命令行才能看到面板。

## 性能要求
- 历史回放和鼠标交互必须流畅。
- 不要在浏览器每次 hover 时重新扫描全部历史订单。
- 可以预计算，也可以设计索引、缓存、后端查询或其他方式，只要体验流畅。
- 如果使用本地数据库，查询结构要方便根据时间快速拿到对应状态。
- **特别提醒**：如果用 lightweight-charts 这类库为每条挂单线创建独立 series，hover 时每秒会触发几十次 crosshair 事件，简单"每次都 add/remove series"的实现会让浏览器卡死。要做：(1) 检测选中 K 线没变就直接返回；(2) 用 requestAnimationFrame 节流；(3) 用 series pool 复用，常驻 series 只更新 data/options 而不是反复 add/remove。
- **"显示全部订单"模式**一次要画上千条线段，渲染开销大。它的内容只跟数据有关、跟选中时刻无关，所以**开启时只需渲染一次**，之后 hover / 选中时刻变化不应该触发重画（否则每次 hover 都重建上千个 series 会卡死）。注意把这个总览模式和"随选中时刻变化的单时刻视图"在重绘路径上分开。

## 更新和去重
- 项目应该支持每周更新一次。
- 每次更新都应该保存原始数据证据。
- 重复运行同一次更新不应该产生重复记录。
- 对已经完结的历史订单，重复数据应该能安全去重。
- 对尚未完结的 open order，要考虑它之后可能更新、成交或撤单。
- 要保留足够信息，让未来可以知道某笔订单第一次看到、最后一次看到、何时变成成交或撤单。
- hyperliduiq只会保留一定数量的历史order, 所以历史数据很珍贵要一直存好

## 数据正确性和校验
- 当前仓位必须能和 Hyperbot 或 Hyperliquid 官方 API 校验。
- 如果页面显示与 Hyperbot 不一致，要优先排查：
- 是否最新数据没更新。
- 是否 `fills` 方向解析错误。
- 是否把 `Long > Short`、`Short > Long` 这类方向处理反了。
- 是否把 reconstructed 历史仓位和 clearinghouse 当前真实仓位混在一起。
- 对数据窗口限制、推断逻辑和无法精确还原的地方要诚实说明。
- **historicalOrders 同一 oid 多条记录陷阱**（实施时被反复踩中）：Hyperliquid 的 `historicalOrders` 对同一个 `oid` 可能返回两条记录——一条 `status='open'`（订单创建那一刻）+ 一条终态（`filled` / `canceled` / `marginCanceled` / `triggered` / `rejected`），而且**两条的 `statusTimestamp` 完全相同**（订单瞬时成交时）。upsert 时必须明确让终态优先，**不能简单按 `status_timestamp` 升序覆盖**，否则后写入的 `open` 会把已成交的订单"复活"成幽灵挂单。直观自检：数据库里 `status='open'` 的订单总数应严格等于最近一次 `frontendOpenOrders` 返回的订单数，多出来的就是踩了这个坑。
- **账户价值端到端对账**：提供一个脚本，从全历史 ledger + fills + funding 重建账户价值，跟 API 直接读的 `accountValue` 比对，差额应在很小的容差内（仅 BTC 价格秒级波动 + funding 分段抓取误差）。会计等式：`accountValue ≈ Σ充值 − Σ提现 ± 内部转账 + Σ已实现盈亏(closedPnl) − Σ手续费 + Σ资金费率净额 + 当前未实现盈亏`。这条对账一旦通过，等于端到端证明了 fills / ledger / funding / 持仓数据全都自洽。

## 用户偏好
- 用户中文阅读更快，文档和界面说明优先中文。
- 用户喜欢能直接验证的数据证据，不喜欢只听概念解释。
- 用户更看重准确性和可解释性，而不是花哨界面。
- 但图表交互要顺滑，不能卡顿。
- 命名和文件组织可以自由设计，但最终要让用户一眼知道“该运行哪个入口”。

## 期望交付
- 一个可长期运行的数据抓取和更新流程。
- 一个本地持久化数据库或等价的可靠存储。
- 一个能查看 Paul 历史订单、仓位、杠杆的交互式 dashboard。
- 一份简短说明：如何更新数据、如何打开图表、如何验证当前状态。
- 一个自动化检查或 smoke test，用来确认核心流程没有坏。

## 最终验收标准
- 用户可以每周轻松更新数据。
- 用户可以查看 `1d / 4h / 1h` 不同时间粒度下的 BTC K 线和 Paul 状态。
- 用户可以在任意选中时间点看到 Paul 的挂单、仓位、账户价值和实际杠杆。
- 最新 reconstructed 仓位能和 clearinghouse 当前真实仓位互相校验。
- 图表清楚展示历史杠杆变化。
- 成交量来源、杠杆计算方式、订单百分比分母都清楚标注。
- 系统能长期累计历史，不会因为 API 滚动窗口丢掉已经抓到的宝贵数据。
- 数据库中 `status='open'` 的订单数严格等于最近一次 `frontendOpenOrders` 返回的数量（没有幽灵挂单）。
- 选历史任意时间点：当时已成交 / 已撤销的订单在图上不能有任何视觉残留。
- 主图挂单线段从选中时刻向右延伸（右 = 未来），不贯穿整图；仍在挂的延伸到图右端预留空白处。
- 挂单线段：实线 = 最终成交、点线 = 未成交；图上不叠加文字标签；不存在"推断"标注。
- 四种 fill 箭头（被动买 / 主动买 / 被动卖 / 主动卖）颜色互不相同；提供"只看主动成交"开关；fill 标记文字显示占账户百分比。
- 上下两图（K 线 + 杠杆）严格对齐：数据点序列一致 + 价格轴宽度一致，hover 十字线不错位。
- 提供"显示全部订单"开关，能一次性按真实生命周期总览全部历史挂单，且开启后 hover 不卡顿（只渲染一次）。
- 账户价值端到端对账通过（重建值 ≈ API 真实 `accountValue`，差额在很小容差内）。
- 能从历史 weekly snapshot / fills 反推 / 链上完整归档回补早期订单，覆盖到 Paul 真实首次活动时间。
- 数据根目录可通过环境变量配置到大容量盘；代码里没有任何硬编码绝对路径。
- 一键入口（如 `server.py`）：IDE 里点播放就能起服务并自动开浏览器。
- 鼠标快速 hover 时主图响应流畅，不卡死。
